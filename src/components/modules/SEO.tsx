import React from "react";

interface Props {
  lang?: string;
  meta?: Array<Record<string, string>>;
  description?: string;
  title: string;
}

const SEO: React.FC<Props> = () => null;

export default SEO;
