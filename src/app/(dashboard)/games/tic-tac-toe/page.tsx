import { redirect } from "next/navigation";

const TIC_TAC_TOE_REPO_URL = "https://github.erniefrancisiv.com/Tic-Tac-Toe/";

export default function TicTacToePage() {
	redirect(TIC_TAC_TOE_REPO_URL);
}
